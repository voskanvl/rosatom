mixin range(val, min, max, label, unit)
    .range
        if label
            div= label
        .range__input(min= min, max= max, val= val,  step= attributes.step, unit= attributes.unit, array= attributes.array )
        input(type="text", value= val)
        div= unit